This is specific for building gitea on armv7 32bit limited to 2GB board with a tweek to pnpm because it goes out of memory
use the script to build the last tag, tag it as "latest"

docker-compose up

runs on localhost:3000 without any changes in the compose
